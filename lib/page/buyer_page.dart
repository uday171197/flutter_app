import 'package:flutter/material.dart';

class Buyer_Auction extends StatelessWidget {
  const Buyer_Auction({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}